# Dataset location

Place the supplied CSV at `dataset.csv`. It was not present when the Phase 1 repository scaffold was created. The pipeline must inspect the header and resolve candidate aliases through `column_mapping.json` before using any feature or target.
